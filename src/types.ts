export type DofusWindow = {
    title: string;
    hwnd: number;
    name: string;
    class: string;
};

export type Shortcuts = {
    next: string;
    prev: string;
    click_all: string;
    click_all_delay: string;
};
